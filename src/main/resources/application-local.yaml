spring:
  data:
    mongodb:
      uri: mongodb://root:secret@localhost:27017/stocks?authSource=admin

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

nats:
  url: "nats://localhost:4222"